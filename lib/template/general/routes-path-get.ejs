
  /**
   * @method GET <%= routepath %>
   * 
   * 
   */
  app.get('<%= routepath %>', userAuthRequired, function(req, res, next) {
     
      res.render('<%= routeview %>', {
        link: '<%= navlink %>',
        data: [],   
        currentUser: req.session.adminuser,
      });

  });
