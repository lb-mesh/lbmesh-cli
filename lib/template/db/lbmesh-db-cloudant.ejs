version: '3'
services:

    cloudant:
        image: <%= cloudant_image %>
        container_name: lbm-db-cloudant
        hostname: developer.local
        restart: always
        ports:
        - "<%= cloudant_port %>:80"
        volumes:
        - ./cloudant/data:/srv
        privileged: true
        network_mode: bridge