version: '3'
services:

  mongodb:
    image: mongo:3.6.1
    restart: always
    container_name: lbm-db-mongodb
    volumes:
      - ./mongo/data:/data/db
      - ./mongo/config:/data/configdb
    ports:
        - 27017:27017
    command: mongod
  