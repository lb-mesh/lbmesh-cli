version: '3'
services:

  redis:
    image: <%= redis_image %>
    restart: always
    container_name: lbm-db-redis
    volumes:
        - ./redis/data:/data
    ports:
        - "<%= redis_port %>:6379"