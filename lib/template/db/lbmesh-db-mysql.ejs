version: '3'
services:

  mysql:
    image: <%= mysql_image %>
    container_name: lbm-db-mysql
    restart: always
    volumes:
      - ./mysql/data:/var/lib/mysql
    ports:
      - "<%= mysql_port %>:3306"
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: lbmesh-db-mysql