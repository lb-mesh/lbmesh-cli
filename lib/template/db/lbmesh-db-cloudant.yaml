version: '2'
services:

    cloudant:
        image: ibmcom/cloudant-developer:latest
        container_name: lbmesh-db-cloudant
        hostname: developer.local
        restart: always
        mem_reservation: 128m
        mem_limit: 384m
        ports:
        - "8880:80"
        volumes:
        - "/Users/jamilspain/cloudant/lbmesh.io/data:/srv"
        privileged: true
        network_mode: bridge