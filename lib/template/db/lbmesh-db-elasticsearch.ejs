version: "2"
services:

  elasticsearch:
    image: <%= elasticsearch_image %>
    container_name: lbmesh-integ-elasticsearch
    mem_reservation: 1280m
    mem_limit: 4096m
    ulimits:
      memlock:
        soft: -1
        hard: -1
    environment:
      - "discovery.type=single-node"
      - "node.name=lbmesh-elastic-search"
      - "cluster.name=lbmesh-cluster"
      - "xpack.security.enabled=false"
    volumes:
      - "<%= homedir_data %>:/usr/share/elasticsearch/data"
    ports:
      - "<%= elasticsearch_port %>:9200"
      - "9300:9300"
    networks:
      - lbmesh-integ-elasticsearch

networks:
  lbmesh-db-elasticsearch: