version: '3'
services:

  mongodb:
    image: <%= mongo_image %>
    restart: always
    container_name: lbm-db-mongodb
    volumes:
      - ./mongo/data:/data/db
      - ./mongo/config:/data/configdb
    ports:
        - "<%= mongo_port %>:27017"
    command: mongod