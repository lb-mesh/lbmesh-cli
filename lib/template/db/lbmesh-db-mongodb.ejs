version: '3'
services:

  mongodb:
    image: <%= mongodb_image %>
    restart: always
    container_name: lbm-db-mongodb
    volumes:
      - ./mongo/data:/data/db
      - ./mongo/config:/data/configdb
    ports:
        - "<%= mongodb_port %>:27017"
    command: mongod